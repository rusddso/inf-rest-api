GET        /                       com.infinera.controllers.HomeController.index

->         /v1/accounts            com.infinera.accounts.AccountRouter

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file           controllers.Assets.at(path="/public", file)

GET        /healthcheck            com.infinera.controllers.HealthCheckController.index
